export type ModuleIconId = "grammar" | "vocabulary" | "sat" | "oge";

export type NavIconId = "home" | "vision" | "review" | "stats" | "settings";

export type AstroIconId =
  | ModuleIconId
  | NavIconId
  | "streak-flame"
  | "xp-gem";
